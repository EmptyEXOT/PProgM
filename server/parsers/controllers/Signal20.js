const Controllers = require('../Controllers.js');

class Signal20 extends Controllers{
    constructor(line) {
        super(line)
    }

    parse(line) {
        switch (line) {
        }
    }
}

module.exports = Signal20;