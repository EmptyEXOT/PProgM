import React from 'react';
import classes from "../main.module.css";

const Sidebar = () => {
    return (
            <div className={classes.pprogSidebar}>sidebar2</div>
    );
};

export default Sidebar;