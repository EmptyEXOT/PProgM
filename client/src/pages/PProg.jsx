import React from 'react';
import axios from "axios";

const PProg = () => {

    return (
        <div>
            <button>Upload config</button>
        </div>
    );
};

export default PProg;